\## 2026-09-15

\*\*Что сделал:\*\*

\- Применил миграцию 0001\_initial (Alembic).

\- Исправил models.py: role\_permissions и user\_roles переделаны в Table (M:N через core.Table).

\- Запустил seed.py — созданы роли, permissions, admin-пользователь.

\- Прогнал test\_rbac.py — RBAC работает.



\*\*Что узнал:\*\*

\- relationship(secondary=...) в SQLAlchemy 2.x работает только с Table, не с ORM-классом.

\- Разница между ролями приложения (таблица roles) и PostgreSQL-ролями (app\_admin, app\_user, ...).

\- Как Alembic применяет миграции (alembic upgrade head).



\*\*Проблемы:\*\*

\- NoForeignKeysError → fixed переделкой RolePermission/UserRole в Table.

\- UnicodeDecodeError на Windows → fixed через ?client\_encoding=utf8 в DATABASE\_URL.



\*\*Дальше:\*\* Модуль 2 — аудит и триггеры.



\## 2026-09-15 — МОДУЛЬ 1 ЗАКРЫТ



\*\*Что сделал:\*\*

\- Применил миграцию 0001\_initial (Alembic).

\- Исправил models.py: role\_permissions и user\_roles → core.Table (иначе M:N не работал).

\- Создал PostgreSQL-роли (app\_admin, app\_analyst, app\_user, app\_readonly).

\- Запустил seed.py: 4 роли, permissions, admin-пользователь.

\- Прогнал test\_rbac.py — работает.

\- Разобрался с Git: "случайный" .git в C:\\Users\\iona3 мешал, создал свой в проекте.

\- Запушил чисто на GitHub: github.com/iona333666777-oss/sentinelDB.



\*\*Что узнал:\*\*

\- relationship(secondary=...) в SQLAlchemy 2.x работает только с core.Table.

\- git rev-parse --show-toplevel — как найти корень репозитория.

\- Никогда не делать git init в домашней папке.

\- .env должен быть в .gitignore — иначе пароль утечёт.



\*\*Проблемы и решения:\*\*

\- NoForeignKeysError → переделал RolePermission/UserRole в Table.

\- UnicodeDecodeError → добавил ?client\_encoding=utf8 в DATABASE\_URL.

\- Git видел всю домашнюю папку → создал отдельный .git в проекте, старый удалил.



\*\*Дальше:\*\* Модуль 2 — аудит и триггеры.



\## 2026-09-15 — Модуль 2 закрыт



\*\*Что сделал:\*\*

\- Миграция 0003\_fix\_audit\_log: переделал audit\_log под ТЗ (11 колонок).

\- Создал функцию log\_action() на PL/pgSQL, 4 триггера аудита + защита append-only.

\- Реализовал 5 функций в src/db/audit.py, включая get\_events\_by\_ip().

\- Прогнал test\_audit.py — зелёный.

\- Проверил вручную: event\_time, client\_ip=127.0.0.1, txid, old\_data/new\_data пишутся.



\*\*Что узнал:\*\*

\- AFTER-триггеры + SECURITY DEFINER + JSONB — основа аудита в БД.

\- inet\_client\_addr() и txid\_current() дают контекст события.

\- append-only через BEFORE UPDATE/DELETE триггер с RAISE EXCEPTION.

\- GPT-кодеры часто отклоняются от ТЗ — структуру нужно проверять лично.



\*\*Проблемы и решения:\*\*

\- Первая версия audit\_log имела user\_id вместо username и без client\_ip.

&#x20; Исправлено миграцией 0003.



\*\*Дальше:\*\* Модуль 3 — шифрование полей и RLS.

